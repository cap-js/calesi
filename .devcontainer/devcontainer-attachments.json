{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "customizations": {
    "codespaces": {
      "repositories": {
        "cap-js/incidents-app": {
          "permissions": "read-all"
        },
        "cap-js/attachments": {
          "permissions": "read-all"
        }
      }
    }
  },
  "postCreateCommand": "git submodule update --init && git clone https://github.com/cap-js/attachments.git plugins/attachments && npm i && npm i -g @sap/cds-dk",
  "postStartCommand": "cd samples/attachments && cf login -a https://api.cf.eu12.hana.ondemand.com -o cap-enablement-team -s samples && cds bind @cap-js/attachments -2 attachments-sample:attachments-sample-keycds watch sample --profile hybrid"
}
